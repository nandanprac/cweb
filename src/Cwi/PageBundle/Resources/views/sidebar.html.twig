{% block sidebar %}

    <div class="custsidebar">
        <h4 class="sidebarh4">Get answers about your health also!</h4>
        <a href="/app/download"type="button" class="btn btn-warning sidebarbtn uppercase">Ask Doctors Now</a> </button>
        <p class="fw6 greymid sidebarp"> Health Topics</p>

        {% for topic in topics %}
            {# <button type="button" class="btn btn-default sdbtn">{{ topic }}</button> #}
            <a href="{{ weburl }}#/category/{{ topic.url | lower }}" class="btn btn-default sdbtn">{{ topic.topictext }}</a>

            {# href="#/category/{{ topic | lower }}" #}
        {% endfor %}

    </div>



{% endblock %}